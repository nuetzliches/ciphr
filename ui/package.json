{
  "name": "ciphr-ui",
  "version": "0.4.2",
  "private": true,
  "description": "Read-only viewer for a ciphr deployment: audit trail, secret metadata, identities, policies, health.",
  "license": "MIT OR Apache-2.0",
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "notices": "node scripts/build-notices.mjs",
    "typecheck": "vue-tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "3.5.41"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "6.0.8",
    "typescript": "5.9.3",
    "vite": "8.2.1",
    "vue-tsc": "3.3.10"
  }
}
