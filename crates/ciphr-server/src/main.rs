#![forbid(unsafe_code)]

//! Entry point of the ciphr server.

fn main() {
    // Phase 0: the workspace layout and the CI gates come first. The listener,
    // configuration loading, and the refusal to start without an audit device
    // arrive in phase 3.
}
