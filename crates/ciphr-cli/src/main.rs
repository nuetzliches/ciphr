#![forbid(unsafe_code)]

//! Entry point of the `ciphr` command-line interface.

fn main() {
    // Phase 0: the workspace layout and the CI gates come first. Commands
    // arrive in phase 3, on top of the SDK.
}
